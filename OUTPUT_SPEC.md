# Idea Seed 输出规范

> 需求文档和技术方案的格式设计，让 Claude Code 能清晰理解"做什么"和"怎么做"

---

## 核心原则

1. **结构化**：Claude Code 是代码智能体，需要清晰的条目和标记
2. **可执行**：文档内容可直接转化为开发任务
3. **分层**：从高层概述到具体实现，层层递进
4. **原子化**：每个功能点独立描述，便于追踪

---

## Requirements.md 结构

```markdown
# 需求文档：{项目名称}

> 种子想法：{原始种子描述}
> 版本：{round}
> 状态：草稿/评审中/定稿

## 1. 项目概述

### 1.1 核心价值
{一句话描述这个项目解决什么问题}

### 1.2 目标用户
{目标用户群体}

### 1.3 成功标准
- [ ] {可验证的指标1}
- [ ] {可验证的指标2}

---

## 2. 功能需求

### 2.1 核心功能

#### F1: {功能名称}
- **描述**：
- **用户故事**：
  作为一个 {角色}，我希望 {功能}，以便 {价值}
- **验收标准**：
  - [ ] {可测试的标准1}
  - [ ] {可测试的标准2}
- **优先级**：P0/P1/P2
- **依赖**：F{相关功能编号}

#### F2: {功能名称}
...

### 2.2 用户交互流程

```
{角色}
  │
  ▼
[操作1] → [系统响应1] → [操作2] → [系统响应2]
```

---

## 3. 数据需求

### 3.1 数据实体

#### User
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | string | ✓ | 唯一标识 |
| name | string | ✓ | 用户名 |
| email | string | ✓ | 邮箱 |

### 3.2 存储方案
- **持久化**：{存储方式}
- **缓存**：{缓存策略}

---

## 4. 非功能需求

### 4.1 性能
- 响应时间 < {X}ms
- 并发支持 {N} 用户

### 4.2 安全
- {安全要求}

### 4.3 可用性
- {可用性要求}

---

## 5. Out of Scope

以下功能不在本版本范围内：
- {明确排除的功能}

---

## 6. 任务清单

### 阶段一：核心功能
- [ ] T1: {任务描述}
- [ ] T2: {任务描述}

### 阶段二：增强功能
- [ ] T3: {任务描述}

---

## 7. 评审记录

| 轮次 | 日期 | 评审结果 | 主要反馈 |
|------|------|----------|----------|
| R1 | 2024-01-01 | 需修改 | {反馈摘要} |
| R2 | 2024-01-02 | 通过 | - |
```

---

## Tech-Design.md 结构

```markdown
# 技术方案：{项目名称}

> 对应需求：{requirements.md 版本}
> 版本：{round}
> 状态：草稿/评审中/定稿

## 1. 技术栈

| 组件 | 技术选型 | 替代方案 | 选择理由 |
|------|----------|----------|----------|
| 语言 | Python 3.x | Go/Rust | {理由} |
| 框架 | FastAPI | Flask/Django | {理由} |
| 数据库 | SQLite | PostgreSQL | {理由} |

---

## 2. 系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Client                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      API Layer                               │
│  /api/v1/users    /api/v1/items    /api/v1/orders          │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   User Svc      │  │   Item Svc      │  │  Order Svc      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Data Layer                              │
│                    SQLite + SQLAlchemy                       │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
{项目名称}/
├── api/
│   ├── __init__.py
│   ├── main.py           # FastAPI 入口
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── users.py
│   │   ├── items.py
│   │   └── orders.py
│   └── schemas/           # Pydantic 模型
│       ├── __init__.py
│       ├── user.py
│       └── ...
├── models/                # SQLAlchemy 模型
│   ├── __init__.py
│   ├── user.py
│   └── ...
├── services/              # 业务逻辑
│   ├── __init__.py
│   └── user_service.py
├── core/                  # 核心配置
│   ├── __init__.py
│   ├── config.py
│   └── database.py
├── tests/
│   └── ...
├── requirements.txt
└── README.md
```

---

## 3. 接口设计

### 3.1 用户接口

#### 创建用户
```
POST /api/v1/users

Request:
{
  "name": "string",
  "email": "string"
}

Response (201):
{
  "id": "uuid",
  "name": "string",
  "email": "string",
  "created_at": "datetime"
}
```

#### 获取用户
```
GET /api/v1/users/{user_id}

Response (200):
{
  "id": "uuid",
  "name": "string",
  "email": "string",
  "created_at": "datetime"
}
```

---

## 4. 数据模型

### 4.1 User 表

```sql
CREATE TABLE users (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 5. 关键实现细节

### 5.1 {关键功能1}

**实现方案**：
```
{伪代码或流程描述}
```

**关键代码**：
```python
# services/user_service.py
async def create_user(name: str, email: str) -> User:
    # 1. 验证邮箱格式
    # 2. 检查邮箱唯一性
    # 3. 创建用户
    # 4. 返回结果
```

**测试策略**：
- 单元测试：验证业务逻辑
- 集成测试：验证数据库交互

---

## 6. 配置管理

### 6.1 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| DATABASE_URL | sqlite:///./app.db | 数据库连接 |
| LOG_LEVEL | INFO | 日志级别 |

### 6.2 配置文件

```yaml
# config.yaml
app:
  name: "idea-seed"
  version: "1.0.0"

database:
  url: "${DATABASE_URL}"

server:
  host: "0.0.0.0"
  port: 8000
```

---

## 7. 开发任务

### P0 - 核心功能

| Task | 描述 | 预估时间 | 依赖 |
|------|------|----------|------|
| DEV-1 | 项目初始化，目录结构 | 0.5d | - |
| DEV-2 | 数据库模型和连接 | 1d | DEV-1 |
| DEV-3 | 用户 CRUD API | 2d | DEV-2 |
| DEV-4 | 核心业务逻辑 | 3d | DEV-3 |

### P1 - 重要功能

| Task | 描述 | 预估时间 | 依赖 |
|------|------|----------|------|
| DEV-5 | XX 功能 | 2d | DEV-4 |
| ... | ... | ... | ... |

---

## 8. 风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| {风险1} | {影响} | {概率} | {应对} |

---

## 9. 评审记录

| 轮次 | 日期 | 评审结果 | 主要反馈 |
|------|------|----------|----------|
| R1 | 2024-01-01 | 需修改 | {反馈摘要} |
| R2 | 2024-01-02 | 通过 | - |
```

---

## 文档间关系

```
种子想法
    │
    ▼
requirements.md (做什么)
    │ 引用
    ▼
tech-design.md (怎么做)
    │ 指导
    ▼
Claude Code 开发
    │ 实现
    ▼
实际代码
```

---

## Claude Code 友好的关键设计

### 1. 任务清单（Task List）

```markdown
### 待办任务
- [ ] T1: 初始化项目目录结构
- [ ] T2: 实现数据库连接
- [ ] T3: 创建用户 CRUD 接口
```

**理由**：Claude Code 可直接通过 `[ ]` 状态追踪进度

### 2. 验收标准（Acceptance Criteria）

```markdown
#### F1: 用户注册
验收标准：
- [ ] 邮箱格式正确时创建成功
- [ ] 邮箱格式错误时返回 400
- [ ] 邮箱重复时返回 409
- [ ] 响应包含用户 ID
```

**理由**：每条都是可执行的测试断言

### 3. 依赖标记

```markdown
#### F2: 用户登录
依赖：F1 (用户注册)
```

**理由**：明确任务执行顺序

### 4. 代码模板

```markdown
### 关键实现：用户服务

```python
# services/user_service.py
async def create_user(name: str, email: str) -> User:
    """创建用户

    Args:
        name: 用户名
        email: 邮箱

    Returns:
        User: 创建的用户对象

    Raises:
        ValueError: 邮箱格式不正确
        DuplicateEmailError: 邮箱已存在
    """
    # TODO: 实现
```
```

**理由**：提供代码结构，减少 Claude Code 的猜测

### 5. API 规范

```markdown
### POST /api/v1/users

请求体：
```json
{
  "name": "string (必填, 1-100字符)",
  "email": "string (必填, 有效邮箱格式)"
}
```

响应：
- 201: 创建成功，返回用户对象
- 400: 参数校验失败
- 409: 邮箱已存在
```

**理由**：Claude Code 可直接用这个规范生成代码

---

## 评审输出格式

```markdown
## 评审报告

### 需求评审

**评审对象**：requirements.md v{round}
**评审时间**：{datetime}
**评审结果**：✅ 通过 / ⚠️ 需修改

#### 意图对齐
- [x] 覆盖种子想法核心诉求
- [x] 需求无遗漏
- [ ] 需补充：{具体建议}

#### 完整性
- [x] 功能需求完整
- [x] 非功能需求明确
- [ ] 需补充：{具体建议}

#### 可行性
- [x] 需求可实现
- [ ] 风险项：{具体建议}

### 改进建议

1. {具体可操作的建议}
2. {具体可操作的建议}
```

---

## 迭代总结格式

```markdown
# 迭代总结

## 会话信息
- 种子想法：{seed}
- 开始时间：{start_time}
- 结束时间：{end_time}
- 总迭代轮次：{n} 轮

## 需求阶段
- 迭代轮次：{n} 轮
- 收敛原因：{通过/达到上限}
- 最终版本：requirements.md v{final_version}

## 方案阶段
- 迭代轮次：{n} 轮
- 收敛原因：{通过/达到上限}
- 最终版本：tech-design.md v{final_version}

## 产出文档
- requirements.md
- tech-design.md
- iteration_summary.md

## 关键决策
1. {决策点}：{理由}
2. ...

## 未解决问题
- {问题描述}
- 建议：{处理建议}
```
